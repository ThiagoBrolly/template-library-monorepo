{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["master"], 
      "message": "chore(release): publicando versão",
      "noCommitHooks": true,
      "ignoreChanges": ["yarn.lock"],
      "conventionalCommits": true
    }
  }
}
